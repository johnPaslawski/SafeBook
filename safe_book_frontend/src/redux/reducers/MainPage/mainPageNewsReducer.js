let initializeState = () => ({
    mainPageNews : {}
});

const mainPageNewsReducer = (state, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}