@model LoginViewModel

@{
    Layout = "_Layout";
    ViewData["Title"] = "Login";
}

<div class="d-flex justify-content-center mt-5">
    <div class="w-25">
        <form asp-controller="Auth" asp-action="Login" method="post">
            <input type="hidden" asp-for="ReturnUrl" />
            <div>
                <label>Nazwa użytkownika / e-mail</label>
                <input asp-for="Username" class="form-control"/>
            </div>
            <div>
                <label>Hasło</label>
                <input asp-for="Password" class="form-control"/>
            </div>
            <div class="mt-2">
                <button type="submit" class="btn btn-dark">Zaloguj</button>
            </div>
        </form>
        <div class="mt-2">
            @if (Model?.ErrorMessage != null)
            {
                @Model.ErrorMessage
            }
        </div>

        <div class="mt-2">
            <a asp-action="Register" asp-controller="Auth"
               asp-route-returnUrl="@Model.ReturnUrl">Utwórz konto</a> @* remember not to loose the return url!!!*@
        </div>
    </div>
</div>